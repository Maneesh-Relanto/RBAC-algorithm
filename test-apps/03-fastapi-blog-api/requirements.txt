# FastAPI Blog API — runtime dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
PyJWT>=2.8.0
bcrypt>=4.0.0

# HTTP client for tests (replaces requests; also used by TestClient internally)
httpx>=0.27.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
